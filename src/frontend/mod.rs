//! The `winit` + `softbuffer` frontend: window, 60 fps loop, render, input
//! (port of `sdl-main.c`).
//!
//! Implemented in milestones 5 and 6.

/// Run the emulator GUI. Entry point called by the `risc` binary.
pub fn run() {
    unimplemented!("frontend lands in milestone 5");
}
