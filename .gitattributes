# Vendored Project Oberon / Norebo toolchain assets are embedded byte-for-byte via
# include_bytes! (and the .Mod sources happen to use CRLF). Mark them -text so git
# never normalizes line endings, keeping `build-image` bit-reproducible.
crates/host-tools/assets/Norebo/**     -text
crates/host-tools/assets/Bootstrap/**  -text
